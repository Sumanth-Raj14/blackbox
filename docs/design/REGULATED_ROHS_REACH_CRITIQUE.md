**Codebase-fit note first:** the spec's line-anchored references check out against the findings â€” head `041_part11_esignatures`, `bom_items_master`/`bom.py:61`, `BomClosure`, `_compute_levels_and_effective_qty` (749-787), `get_cost_rollup` (838-886), `_invalidate_bom_caches` (123-127), `TenantAwareMixin`, `part11_service.sign_action` (45-98), `eco_service` (186-302), `part.py:94/95`, RLS 040/041 pattern, frontend ComplianceScreen honesty gap (42-50, 52-80), drawer tabs (123-144), CostRollupView clone, doubled-prefix. Regulatory core (RoHS-10 + Cd@0.01%, homogeneous-material basis, SVHC >0.1%/article, O5A non-dilution, Annex III/IV, no per-part SVHC exemption, versioned list never hard-coded) is accurate. Issues below, prioritized.

---

**P1 â€” HIGH Â· reference data is tenant-scoped AND seeded inside migration 042.** All reference models subclass `TenantAwareMixin` (NOT-NULL `tenantId`, `uq(tenantId,â€¦)`), yet 042's data step reads JSON and inserts with no tenant in context â†’ NULL `tenantId` violation, and any tenant created *after* 042 gets zero reference data. It also duplicates a *global* regulatory fact (RoHS-10, ~253 SVHC, Annex III/IV) per tenant with drift risk. *Fix:* make regulatory master data global (separate non-tenant base, or system rows with nullable tenant) and keep only tenant-specific imports tenant-scoped; OR drop the in-migration seed and provision every tenant via the idempotent `/reference/seed` at tenant-creation. The "042 data step **or** seed endpoint" hand-wave must be resolved to the endpoint.

**P2 â€” HIGH Â· NULLs in unique constraints don't enforce uniqueness.** Postgres/SQLite treat NULL as distinct, so `ComplianceEvaluation uq(...,bom_id,basis)` with `bom_id=NULL` permits **duplicate SELF evaluations**; same for `RestrictedSubstanceEntry(substance_id,substance_group_id)` (one always NULL) and `RohsExemption`. The spec already hedges `is_current` ("partial-unique or app-enforced") but doesn't apply the fix elsewhere. *Fix:* partial unique indexes (`WHERE bom_id IS NULL`, `WHERE is_current`) or COALESCE sentinels on every nullable-key uq.

**P3 â€” HIGH Â· exemption applicability is end-product-category-dependent, but SELF eval is deduped once per part.** RoHS exemptions apply per EEE category of the *finished product*; the same shared component can be exempt in a cat-8 device but non-compliant in a cat-1 device. The design puts `eee_category` on the Part and evaluates one context-free SELF status (dedupe on `part_id`, `bom_id=NULL`) â€” it cannot represent this, and leaf parts will have `eee_category=NULL` so `part.eee_category âˆˆ applicable_categories` silently never matches. *Fix:* resolve EEE category from assembly/end-product context during ROLLUP (exemption applicability decided in-context, not on the deduped SELF row); require category on the top assembly and inherit.

**P4 â€” HIGH Â· no cold-start/backfill plan.** Existing parts have only free-text `Part.material` and no composition, `is_article` defaults **True** (mis-flags every existing assembly/phantom as an article), `eee_category` NULL. Day one: every part â†’ `UNKNOWN`, every BOM rolls up `UNKNOWN`. *Fix:* in 043 set `is_article=False` for ASSEMBLY/PHANTOM/kind, seed `eee_category`; add a bulk composition importer and explicitly triage the all-UNKNOWN baseline rather than shipping a wall of red/gray.

**P5 â€” MED Â· `NOT_APPLICABLE` is in the lattice/rollup but no evaluator produces it.** RoHS is EEE-only; REACH applies to articles (not bulk substances/mixtures). Without a derivation rule, non-EEE parts never become N/A and pollute rollups as UNKNOWN. *Fix:* define N/A rules (RoHS N/A when part is non-EEE / out-of-scope; REACH N/A when `is_article=False` bulk material) feeding the "all-descendants-N/A" parent rule.

**P6 â€” MED Â· declaration `valid_until` expiry missing from invalidation triggers.** F1 says "expired declaration â†’ UNKNOWN," but the invalidation list covers exemption expiry, not declaration expiry â€” a naturally-lapsing declaration leaves a stale `COMPLIANT` cached. *Fix:* add declaration `valid_until` crossing `today` to the same time-based sweep as exemption expiry.

**P7 â€” MED Â· structural BOM edits invalidate the cache key but not persisted ROLLUP rows.** Reparent/add/delete changes rollups without any composition change; the spec only adds `bom:substance_rollup:{bom_id}` to `_invalidate_bom_caches`. Persisted `ComplianceEvaluation(basis=ROLLUP)` for affected ancestors goes stale. *Fix:* purge/mark-stale ROLLUP evaluations for affected ancestors in the `_closure_add/reparent/remove` paths.

**P8 â€” MED Â· missing article-mass reconciliation invariant.** REACH per-article % = Î£(SVHC mass across the part's materials) Ã· article mass. The spec validates substances sum â‰ˆ100% *within a material* but never that Î£`PartMaterial.mass_g` â‰ˆ `Part.weight`, and allows `mass_g` OR `mass_fraction`. Without reconciliation the article denominator is unverified and F2 can silently mis-compute. *Fix:* add Î£-materials-vs-part-mass tolerance check; define resolution when only `mass_fraction` is given.

**P9 â€” MED Â· doubled-prefix client vs clean new router.** Existing `api.compliance` hits `/compliance/compliance/â€¦` (api.js:1004-1006); the spec both mounts the new router cleanly at `/compliance/<resource>` *and* "extends/reuses `api.compliance.parts.status`." Reused methods will 404 against the clean routes, and new methods added "in the same style" risk inheriting the double. *Fix:* correct the client base once (accepting it touches existing endpoints/server mounts) â€” you can't keep the double for old calls and clean paths for new on one client object.

**P10 â€” MED Â· certificate storage underspecified.** `document_uri`+`content_hash` exist, but no findings confirm the "existing attachment mechanism," there's **no multipart upload endpoint** for the declaration document (only `/reference/import` is multipart; declarations CRUD is JSON), and hash computation (client vs server) is undefined. *Fix:* verify/define the local file-store service, add a declaration-document upload endpoint, compute SHA-256 server-side on ingest.

**P11 â€” MED Â· rollup performance / N+1 unaddressed.** Self-status for every distinct part loads composition + matches restricted entries; the spec caches results but never specifies batch loading, and a `regulation_version` bump invalidates evaluations catalog-wide. *Fix:* load all items/parts/composition/entries in one pass (mirror `get_cost_rollup`'s single load; replicate its `float` cast for Decimal safety), keep the Â§10.3 lazy re-eval, and bound the version-bump blast radius (only FMD-based, on-demand).

**P12 â€” MED Â· UNKNOWN tone inverts lattice severity.** `COMPLIANCE_TONE` maps `UNKNOWN:"neutral"` (gray) but `COMPLIANT_WITH_EXEMPTION:"warning"` (amber) â€” yet UNKNOWN *outranks* exemption in severity ("absence of data â‰  compliance" is the core guard). Gray reads as benign. Also "extended" is inaccurate â€” it's a parallel status-keyed map, not the existing `valid/expiring/expired/missing` one. *Fix:* give UNKNOWN an attention-getting tone (info/distinct), above exemption visually.

**P13 â€” LOW Â· unverified codebase names.** `SubstanceDeclaration.vendor_id FKâ†’vendors` and `Part.part_kind` ("optional if an existing type field suffices") are not confirmed in findings (domain uses `supplier`; backend never lists a Part `type`). *Fix:* verify `vendors` vs `suppliers` table name and whether Part already has a type/kind column before adding.

**P14 â€” LOW Â· i18n only on the drawer tab label.** Only `__t("detailDrawer.compliance")` is shown; RollupView columns, dashboard KPIs, modal fields, status labels, and toasts are hardcoded English, with no locale number/date/ppm formatting. *Fix:* add translation keys for all new surfaces + locale-aware formatting.

**P15 â€” LOW Â· RENEWAL_PENDING should stay covered, not UNKNOWN.** Regulatory reality: a timely-filed renewal keeps the exemption valid pending Commission decision. Spec flips past `valid_until` to "NON_COMPLIANT (or UNKNOWN if RENEWAL_PENDING)"; UNKNOWN understates a still-valid state. *Fix:* treat RENEWAL_PENDING as `COMPLIANT_WITH_EXEMPTION` (flagged "pending"), configurable.

**P16 â€” LOW Â· exemption modeling granularity.** `RohsExemption uq(tenant,code)` + single `valid_until` can't represent per-category differentiated expiry (domain: validity "may differ per category"). Also `ExemptionClaim` carries `substance_id` only, but RoHS entries can be group-based â€” claim-side matching must resolve group membership too. *Fix:* code+category granularity (or per-category expiry map); group-aware claim matching.

**P17 â€” OVER-SCOPE Â· user CSV-import â†’ new-`RegulationVersion` machinery.** `set-current` flip + lazy re-eval cascade + import provenance is a lot for a first ship when the bundled snapshot suffices; "metal-compound SVHC families" group seeding is open-ended. *Fix:* consider deferring user import (bundled-seed only) to a follow-on; scope group seeding to PBB/PBDE/4-phthalates/"lead compounds" as Â§10.8 already recommends.
